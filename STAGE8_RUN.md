# Run Ember Stage 8

```bash
node --version
npm start
```

Open `http://localhost:8787`.

## Optional NASA FIRMS setup

Copy `.env.example` to `.env`, then add your private map key:

```text
FIRMS_MAP_KEY=your_private_key_here
```

Restart `npm start` after changing `.env`. The key stays on the Node backend.
